# TEST_SPEC.md — kozu-lab(構図ラボ)

<!-- scaffold template v1.24.0 から展開(2026-08-31) -->

## 実行規約

- **TS 層**(`npm test` = `vitest run`)を stage 3–5 の判定に使用。マーカー相当はディレクトリで分ける
  (`tests/unit/` / `tests/integration/` / `tests/validation/`)
- **Rust 層**(`cargo test --manifest-path rust/Cargo.toml`)。二実装照合(G-06a/b/c)は
  Rust 側が固定フィクスチャ(入力 + 出力)を JSON に落とし、TS 側が読んで突き合わせる。
  `cargo fmt --check` / `cargo clippy -- -D warnings` / `wasm32-unknown-unknown` リリースビルドも完了条件
- **検証工程のコマンドは終了コードを直接見る**(HC-103)。出力を整形したいときはファイルへ落とし、
  `exit` を別に確かめる —— パイプを挟むと合否が末尾コマンドのものに化ける
- **Python 層**(`python -m pytest harness/tests -x -q`)。字種検査(G-12)と ④ の事前計算の検算
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(直交性・一意性・単一帰属等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

合成フィクスチャだけでなく、**外部データ・外部権威に対する期待値**にも同じ規律が要る。
ケースごとに、その期待値がどこから来たのかを書くこと。

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない**(超えるなら先に SPEC を上げる) |
| 外部権威(公表値・規格・検証データ) | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す。「誰かの仮定」と区別できるようにする |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。
外部データは重複や欠落を含むため、数はデータが動くたびに壊れる。

### このプロジェクト固有の禁止

**名画に対する検出結果を期待値にしてはならない。**
「『最後の晩餐』の消失点はキリストの右目にある」「『アテナイの学堂』は中央消失点を持つ」は
**通説であって外部権威ではない**(SPEC §6)。これらを期待値に書くと、通説を再生産する装置になる。
名画は**適用対象**であって、オラクルではない。

同じ理由で、**当てはまりスコアの期待値を「黄金比だから高いはず」から書いてはならない**。
スコアの正しさは §「オラクルの出所」の合成フィクスチャに対してのみ主張する。

## オラクルの出所

| フィクスチャ | 出所 | 性格 | 使うゲート |
|---|---|---|---|
| `drawLines({mode:"step"})` | **自作。**既知の (θ, ρ) の**領域境界**を半平面の排他的論理和で描画。θ・ρ は生成器の引数であり、画像から読み戻さない。ρ は θ 依存の支持関数に対する比(HC-100) | 解析解 | G-01 / G-02 / G-13 |
| `drawLines({mode:"stroke"})` | **自作。**距離場 \|x cosθ + y sinθ − ρ\| ≤ 0.5 で細線を描画。ラスタライザ自身の検算にだけ使う。**エッジが両側に 2 本立つので G-01 のオラクルにはしない** | 解析解 | T-001 |
| `renderFloorCheckerboard(cam)` | **自作。**既知のカメラ行列で床平面の市松模様を描く。**セル境界がちょうど 3D 直線の投影**になりステップエッジとして立つ。真の消失点は K·R の列ベクトルから解析的に出る | 解析解 | G-07b / G-07c |
| `renderAffineCheckerboard(...)` | **自作。**平行投影の市松模様。消失点が無限遠にあることが定義から分かる | 解析解(破綻side) | G-07c |
| `fx/grid_positive/*.png` | **自作。**主要素の重心を厳密に t = 1/3・0.382・0.618 の交点へ置いた合成画像 | 陽性対照 | G-08 |
| `fx/noise_pink/*.png` | **自作。**1/f スペクトルのノイズ(シード刻印) | 陰性対照 | G-08 / G-09 |
| `fx/random_grid/*.json` | **自作。**線数 4 / 8 / 20 のランダム配置格子(シード刻印) | 帰無分布 | G-05 |
| `tests/fixtures/rust_oracle.json` | **Rust 実装が書き出す固定フィクスチャ**(`cargo run --release --bin oracle`)。入力(エッジ座標・角度表)も出力(投票・極大・raw/z)も f64 のビットパターンで持つ。**97×73 の奇数寸法**にしてあるのは、偶数だと dx が常に整数になり丸めの流儀を区別できないため(HC-070) | 二実装照合 | G-06a / G-06b / G-06c |
| `tests/fixtures/brokenHough.ts` | **自作。**投票累積を θ ビン 1 本に潰した版。**発火する性質(真値の θ が 0 から離れていること)を対照側で表明してある**(HC-070) | 陽性対照(検査が働くこと) | G-02 |
| `fx/saliency/` | **外部。**公開視線計測データセット(採用可否と出典 URL・取得日は L5 で確定) | 外部権威 | G-目玉2 |
| `data/frames/met-ep-catalog.json` | **実測。**Met dept 11 の **全 2,644 件のメタデータ**。枠の全体が分かっていないと抽出の偏りを確かめられない | 枠の全体 | F-14 |
| `data/frames/met-ep.json` | **実測。**適格条件(PD / 画像あり / classification=Paintings)で 2,029 件に絞り、世紀の層別・シード 20260831 で 200 件を抽出。**規則と取得日を同ファイルに刻む** | 標本枠 | F-14 / G-09 |
| `data/measurements/resolution.json` | **実測。**web-large と original(短辺 1024)の比較 12 件。§3.4 の決着の根拠 | 測定 | SPEC §3.4 |

**合成フィクスチャは画像を配らず、生成器と引数を配る**(N-03 と同じ理由。再現性も上がる)。
生成器は `tests/fixtures/gen_*.ts` に置き、決定論であること(G-04)を自分自身のテストで確かめる。

## ケース一覧

### L1 — 器械の較正

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-02 | 線分生成器が引数どおりの (θ, ρ) を描く。**画像から読み戻さず**、描画した画素が解析的な直線の式を満たすことで検算する | 全画素が \|ax+by+c\| ≤ 0.5 |
| T-002 | F-01 / G-01 | 無劣化の単一線分 × θ を 0–179° で 1° 刻み、ρ を 5 段階 | Δθ ≤ 1.0° かつ Δρ ≤ 短辺の 0.5 %(SPEC G-01) |
| T-003 | F-01 / G-01 | 同、線分 5 本の同時検出。**集合の一致**で書く(件数の定数では書かない) | 真値集合と検出集合が 1:1 対応 |
| T-004 | G-02 | **陽性対照。**投票累積を退化させた検出器で T-002 が落ちる | 落ちること。落ちなければ T-002 ごと捨てる |
| T-005 | G-02 | **陰性対照。**正常な検出器を無劣化フィクスチャ全域に当て、誤検出 0(HC-074) | 真値に対応しない検出直線が 0 本 |
| T-006 | G-04 | 同一入力・同一シードで 100 回、検出結果と z がビット一致 | 不一致 0 件 |
| T-007 | G-03 | **循環の禁止。**検出器・スコア器のソースが真値・フィクスチャ引数を参照していない | 静的走査で 0 件。**陽性対照**(参照する版)が落ちる |
| T-008 | F-04 | 格子生成器が各格子について「線の集合・交点の集合・線数」を返す。交点は線の交わりから**導出**する(定数で書かない) | 導出値と一致 |
| T-009 | F-03 / G-05 | 線数 4 / 8 / 20 のランダム格子に対する z の分布 | \|平均\| ≤ 0.05 かつ \|標準偏差 − 1\| ≤ 0.05(SPEC G-05) |
| T-010 | F-03 | σ を 0.5 % / 1 % / 2 % に振ったとき z が単調に変わらないこと**を主張しない**。σ 依存を記録するだけ | 記録が残ること(測定であって合否ではない) |
| T-011 | G-13 | JPEG 品質 95→30、ノイズ σ 0→20、筆触強度 0→1、額縁幅 0→5 % を段階的に加え、G-01 が落ちる境界を記録 | **閾値なし。**境界が数として記録されること |
| T-012 | F-07 | **幅の選択規則。**`要求幅 = 1024×max(1,W/H) 以上で最小の標準幅`。SPEC §3.2 の 4 作品の原寸(実測 2026-08-31)を入力に、4 件とも **1920** を選ぶ | 1920 ×4 |
| T-012b | F-07 | **白名簿の外を要求しない。**任意の (W, H) 1,000 通りに対し、選ばれた幅が常に 11 幅の集合に属する(SPEC §3.2 の権威 + 実測 104 件) | 集合の包含。**陽性対照**(白名簿を無視する版)が落ちる |
| T-012c | F-07 | **拡大の拒否。**`要求幅 > 原寸幅` の作品(『アテナイの学堂』3820 幅に 3840 を要求する形)を、候補から外すか「短辺不足」として記録する。**黙って拡大画像を解析しない** | 例外か記録が残る |
| T-012d | F-07 | **404 が観測できる経路で問い合わせている。**取得器が非標準幅を組み立てない(非標準幅では不在が 400 に化けて観測できない — SPEC §3.2 実測) | 静的走査 0 件 |
| T-013 | F-08 | 全ページに SPEC §1 の主語の一文が出る | 全ルートで存在 |
| T-014 | G-11 | **層の混線禁止。**規範層の描画が `stroke-dasharray` 無しの線を吐かない | 静的走査 0 件 + **陽性対照**(実線を吐く版)が落ちる |
| T-015 | G-12 | 字種検査 | `harness/text_hygiene.py` が 0 件 |
| T-016 | F-07 / SPEC §3.6 | **解析解像度の正規化。**④ / ⑦ に入る全画像が、短辺 1024 px へ正規化された後の解析結果である。出典が違えば元の解像度が 3 倍違う(Wikimedia 1082–1490 / Met web-large 419 — 実測 2026-08-31) | 未正規化の経路が 0 件。**陽性対照**(正規化を飛ばす版)が落ちる |
| T-017 | F-01 / SPEC §3.6 | **解像度の交絡を測る。**同一作品を短辺 419 / 1024 / 1490 で解析し、検出直線数の差を記録する | **閾値なし。**差が数として記録されること(§3.6 の根拠を自分で持つため) |

### L2 — 二実装照合

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-101 | F-10 / G-06a | Rust と TS で **Hough の投票配列**が一致。入力(エッジ座標・角度表)は固定フィクスチャから与え、両実装とも自前で三角関数を呼ばない | 全要素一致 |
| T-102 | F-10 / G-06a | 同、**極大(θ, ρ)**が一致。refine の重心は四則のみなので完全一致を要求できる | 全要素一致 |
| T-103 | G-06a | **陽性対照。**片側の累積順序をずらした版で T-101 が落ちる | 落ちること |
| T-106 | G-06b | **環境差の測定。**両実装が自前で角度表を計算したとき、投票配列が何箇所ずれるかを記録する(実測 2026-08-31: cos 44/2000・sin 50/2000 が最終 ULP で食い違う) | **閾値なし。**件数と場所が記録されること |
| T-107 | F-10 / G-06c | raw と z の一致。`exp` は 187/2000 で食い違うので完全一致は要求しない | \|Δz\| ≤ 1e-9(SPEC G-06c) |
| T-104 | F-09 | 明度重心が、既知の重心を持つ合成画像で解析解と一致 | 画素誤差 ≤ 0.5 px |
| T-105 | F-09 | 2 値化のしきい値が同一入力で決定論 | 100 回で不一致 0 件 |

### L3 — 消失点

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-201 | F-02 | 透視図生成器の真の消失点が K·R の列から導出でき、投影画素と整合する | 導出と投影が一致 |
| T-202 | F-11 / G-07a | **不確かさの較正。**シードから生成したカメラ 40 台で、真値との誤差が申告 1σ の 3 倍以内に収まる割合を測る | ≥ 95 %(SPEC G-07a / §11.5)。**精度の主張は測って捨てた** |
| T-205 | G-07b | **画素誤差が消失点の遠さとともにどう伸びるかを記録する**(yaw 掃引) | **閾値なし。**表が記録されること |
| T-206 | F-11 | **線束が細いほど不確かさが大きく出る。**遠い消失点の 1σ は近いものより大きい | 単調性。無限遠は Infinity |
| T-203 | F-11 / G-07c | **平行投影の合成画像**(消失点が無限遠)で、推定器が「破綻」を返す | 破綻を返すこと。**有限の点を返したら落ちる** |
| T-204 | F-11 / G-07c | 破綻を平均・補間で埋めていない。材料不足でも埋めない | 静的確認 + T-203 |

### L4 — 帰無仮説の実験室

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-301 | F-12 | t 走査が 181 点([0.05, 0.95] を 0.005 刻み)を過不足なく覆う | 集合の一致 |
| T-302 | F-12 | 順列帰無の 95 % 帯が、**帰無の下で 5 % 程度しか外れない**(帯そのものの検算) | 外れ率 0.03–0.07 |
| T-303 | G-08 | **陽性対照。**P 群(1/3・0.618 交点に厳密配置)で M0 の z ≥ 3.0 | 満たすこと。落ちれば ④ ごと撤回 |
| T-304 | G-08 | **陰性対照。**E 群(1/f ノイズ)で M0 の z が帰無帯の中 | 満たすこと |
| T-305 | G-09 | 各群の実標本数が 175 以上 | N ≥ 175 |
| T-306 | G-10 | A 群と C 群(トリム/回転/鏡像)が**対応あり**で対になっている。対の欠落が無い | 取りこぼし 0 |
| T-307 | F-14 | 標本枠 JSON に抽出規則・乱数シード・取得日が全件揃う | 欠落があれば落ちる |
| T-308 | F-13 | DF 上昇量(M0→M1→M3→M5)を**全群について**測っている | 群 × モードの表が埋まる |
| T-309 | F-13 | 効果量(Cohen's d・Cliff's δ)を、既知の分布対に対して検算 | 解析解と一致 |
| T-310 | F-12 | **t 走査曲線が黄金比を特別扱いしていない。**走査コードに 0.618 / 0.382 / 1/3 の定数が現れない | 静的走査 0 件 |

### L5 — 視線と作品くらべ

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-401 | G-目玉2 | 顕著性マップの AUC-Judd を公開視線データで測る | **≥ 0.75 なら ⑥ を出す。下回れば ⑥ を削除する**。**測った(2026-08-31): 0.6938 → 削除した** |
| T-402 | F-15 | **撤回。**hanshoku-atlas は Asian Art、こちらは European Paintings で objectID は定義上重ならない(SPEC §9.2) | — |
| T-403 | F-15 | **縁の線を落としてから数える。**落とす前後の両方を記録する | 落とした割合が記録されること |
| T-404 | F-15 | **材料の乏しさを記録する** —— 縁を除くと直線が 0 本になる作品の割合 | 閾値なし。数が残ること |

### 全ループ

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-901 | N-01 | ビルド成果物に関数が無い | 0 件 |
| T-902 | N-02 | ネットワーク無しでビルドが通る | 通ること |
| T-903 | N-03 | リポジトリにも `out/` にも美術館の画像が無い | 0 件 |
| T-904 | N-05 | 出荷ビルドが通り、**除外設定で削った後の木でも作れる**(HC-062) | 通ること |
