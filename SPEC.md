# SPEC.md — kozu-lab(構図ラボ)

<!-- scaffold template v1.24.0 から展開(2026-08-31)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

名画に三分割法や黄金分割の格子を重ねた図は、ネット上に無数にある。そのほとんどは、
拡大率・回転・反転・「合っている」と数える特徴を自由に選んでいる。
**十分な自由度があれば、任意の画像に任意の格子を当てられる。**
これは数学史の側から繰り返し出されてきた批判である(Mario Livio の検証が代表的)。

したがって「名画の構図線を自動抽出する」アプリを素直に作ると、その擬似科学の量産機になる。

**本プロジェクトはそこで向きを反転させる。主張は「構図線を見つけること」ではなく、
「構図線がいかに容易に見つかってしまうかを測ること」に置く。**

hanshoku-atlas(版色アトラス)には、媒介を何層も経ているとはいえ、測定対象が物理的に存在した。
**構図線にはそれが無い。**三分割法の線も黄金螺旋も絵の中に描かれておらず、見る側が事後に重ねる格子である。
この非対称が本プロジェクトの設計をすべて決めている。

**本プロジェクトが測るのは「与えられた画像に対し、我々が定義した当てはまりスコアが、
我々が選んだ対照群の分布のどこに立つか」である。**この主語を短くしない。
「この絵は黄金分割で構成されている」は出力しない。

## 2. 二層分離

| | 客観層 | 規範層 |
|---|---|---|
| 実在 | 画像の画素から測れる | 見る側が事後に重ねる |
| 中身 | 直線検出・消失点・明度重心・エッジ角度分布・対称性・顕著性 | 三分割・黄金分割・√2/√3/√5 矩形・対角線法・Hambidge の動的対称・Barnstone のアルマチュア・黄金螺旋 |
| 描画 | **実線・彩度あり** | **破線・低彩度** |
| 凡例語 | 「測定」 | 「重ねた格子」 |
| オラクル | **合成画像で作れる**(§6) | **作れない。だから ④ が要る** |

この描画規則は装飾ではなく **G-11** としてテストで守る。

## 3. 底本と経路

### 3.1 実測(2026-08-31)

| 経路 | 実測 | 判定 |
|---|---|---|
| `images.metmuseum.org`(web-large / original) | 両方 `Access-Control-Allow-Origin: *` | **使う。プロキシ不要** |
| 同(European Paintings の一例 `DP109484.jpg`) | web-large **419×624 / 107 KB**、original **2565×3823 / 2.29 MB**。どちらも **ICC 無し** | §3.4 |
| `collectionapi.metmuseum.org` | `ACAO: *`。`objectID=435848` は `isPublicDomain: true` / `classification: "Paintings"` | メタデータに使う |
| Met 検索 API(`departmentId=11`) | `q=perspective` **87 件**、`q=a` も **87 件**、`q=`(空)も **87 件** | **標本枠にしない**(§3.3) |
| `media.nga.gov`(IIIF) | `200` + `ACAO: *` / `Access-Control-Allow-Methods: *` | **使う** |
| `upload.wikimedia.org` | `ACAO: *`。**サムネイル幅は白名簿制**(§3.2 で確定)。原寸は最大 **228.9 MB** の実例あり | **使う。既定は 1920px(§3.2)** |
| `www.artic.edu/iiif/2/…` | **403 / Cloudflare bot チャレンジ**(hanshoku-atlas が 2026-08-31 に実測) | **画像は使わない** |

### 3.2 Wikimedia は避けて通れない

『最後の晩餐』はミラノ、『アテナイの学堂』はバチカンにあり、**Met にも NGA にも無い。**
② 消失点の説得力はこの二枚に懸かっているので、Wikimedia 経路は必須である。

#### 許容幅の白名簿(確定 2026-08-31)

権威は `https://www.mediawiki.org/wiki/Common_thumbnail_sizes`(`w.wiki/GHai` の転送先)。
`$wgThumbnailSteps` のうち Wikimedia 本番で有効な値は **11 個**である。

```
20, 40, 60, 120, 250, 330, 500, 960, 1280, 1920, 3840
```

同ページの記述:「PHP を経由する要求(imageinfo API 等)は直近の標準幅へ**繰り上げ**られる。
**直接要求(ホットリンク)は標準幅でなければ拒否される**」(phab:T414805)。

**実測(2026-08-31)。**4 作品 ×(標準 11 幅 + 非標準 15 幅)= 104 リクエスト:

| | 幅 | 結果 |
|---|---|---|
| 標準 | 20 / 40 / 60 / 120 / 250 / 330 / 500 / 960 / 1280 / 1920 / 3840 | **44 件すべて 200** |
| 非標準 | 100 / 180 / 300 / 320 / 400 / 640 / 800 / 1024 / 1500 / 2048 / 2560 / 2880 / 3000 / 4000 / 5000 | **60 件すべて 400** |

対象は『最後の晩餐』5193×2926、『アテナイの学堂』3820×2964、
スーラ『グランド・ジャット島』30000×19970、北斎『神奈川沖浪裏』3859×2594。例外ゼロ。

#### 既定幅は 1920 —— 1280 では短辺 1024 に届かない(実測 2026-08-31)

サムネイルは**幅**指定なので、横長の絵ほど短辺が痩せる。実測:

| 作品 | 原寸 | 1280px の短辺 | 1920px の短辺 | 1920px の重さ |
|---|---|--:|--:|--:|
| 最後の晩餐 | 5193×2926 | **721 ✗** | 1082 ✓ | 0.74 MB |
| アテナイの学堂 | 3820×2964 | **993 ✗** | 1490 ✓ | 1.11 MB |
| グランド・ジャット島 | 30000×19970 | **852 ✗** | 1278 ✓ | 0.98 MB |
| 神奈川沖浪裏 | 3859×2594 | **860 ✗** | 1291 ✓ | 0.97 MB |

**1280 は 4/4 で届かず、1920 は 4/4 で届く。**3840 は 2.65–4.12 MB で過剰。
**既定を 1920 とし、選択規則は次のとおり**(F-07):

```
必要幅 = 1024 × max(1, W/H)          W,H は imageinfo の原寸
要求幅 = 必要幅 以上で最小の標準幅
```

#### 原寸は使わない(実測 2026-08-31)

原寸は幅指定が無いので白名簿の外にあり 200 を返すが、**『グランド・ジャット島』は 228.9 MB** である。
閲覧者のブラウザに 229 MB を落とさせる経路は持たない。**原寸は候補から外す。**

> **外れた予測の記録(HC-077)。**本 SPEC の初版(同日)は「原寸は白名簿の外にあるので、
> **原寸を第一候補にし**サムネイルは帯域の都合で使う場合のみ」と書いていた。
> 白名簿を回避することだけを見て、原寸の重さを測っていなかった。実測で 229 MB の実例が出て逆転した。

#### 標準幅は原寸を超えても拒否されず、拡大して返る(実測 2026-08-31)

『アテナイの学堂』は原寸幅 3820 だが、`3840px` は 400 にならず **3840×2980 を返す**。
**拡大補間されたエッジは Canny にとって偽の線源になる。**
取得器は `要求幅 ≤ imageinfo の width` を自分で検算し、満たせない作品は
**候補から外すか、短辺が 1024 に届かないまま解析したことを記録する**(F-07 / T-012)。

#### 非標準幅では 404 が観測できない(実測 2026-08-31)

|  | 標準幅 1920 | 非標準幅 1024 |
|---|---|---|
| 実在ファイル | 200 | 400 |
| 不在ファイル | **404** | 400 |
| ハッシュ不一致のパス | **404** | 400 |

**幅の検査は存在の検査より先に効く。**したがって非標準幅で問い合わせている限り、
ファイルが在るのか無いのかは**原理的に分からない**。
**取得器は必ず標準幅で問い合わせる。**そうして初めて 404 が「候補が悪い」を意味する(T-012)。

### 3.3 Met 検索 API は標本枠にならない(実測 2026-08-31)

`departmentId=11` に対し、`q` を何にしても **total が 87 で頭打ちになる**。
European Paintings 部門の実際の所蔵点数はこれよりはるかに多い。
**検索 API は候補生成にのみ使い、標本枠にしない**(HC-012)。

**ただし枠は別の経路で取れる(実測 2026-08-31)。**
`objects?departmentIds=11` は **2,644 件の objectID を全部返す**。検索 API の頭打ちは
検索エンドポイント固有の性質であって、部門の全件が取れないという意味ではなかった。

> **外れた予測の記録(HC-077)。**本 SPEC は「④ の標本枠は `MetObjects.csv`(317 MB)」と書いていた。
> 検索 API が頭打ちになることだけを見て、**objects エンドポイントを試していなかった**。
> 317 MB の CSV は要らない。

**枠の全体(2,644 件)のメタデータを取って `data/frames/met-ep-catalog.json` に残す** ——
枠の全体が分かっていなければ、抽出が偏っていないことを後から確かめられない。
そのうえで適格条件(`isPublicDomain` / `primaryImage` を持つ / `classification === "Paintings"`)で絞り、
**年代の層に分けてシード固定で抽出**したものを `data/frames/met-ep.json` に刻む。**画像は落とさない**(N-03)。

### 3.7 「名画」を機械可読の印で切れるか(実測 2026-08-31)

A 群(名画)と B 群(一般絵画)を分けるには、**自分の判断でない印**が要る。
Met には `isHighlight` があるが、**dept 11 では 125 件しかない**。
G-09 が要求する **N ≥ 175 に届かない。**

したがって「名画は一般の絵画と違うか」は、**この標本では検定力を確保できない**。
- A 群は「Met European Paintings の PD 絵画から層化抽出した 200 件」= **絵画一般**とする
- `isHighlight` の 125 件は **補助群**として並べるが、**差の有無は主張しない**(実例列挙に格下げ)
- ④ の主張は **A 群 vs C 群(同じ絵の破壊版・対応あり)** と **A 群 vs 合成対照群** に置く

### 3.4 解析解像度は web-large では足りない(実作品で決着 2026-08-31)

web-large の短辺は実測で **297–590 px**(標本枠の先頭 12 件で 304–564)。
§3.6 が定めた正規化(短辺 1024)に**どの作品も届かない**。

**L4 で実作品 12 件を測った**(`data/measurements/resolution.json`)。
同じ作品を web-large と original(短辺 1024 へ縮小)で解析すると:

| | 中央値 | 最大 |
|---|--:|--:|
| 検出直線の一致率(Δθ ≤ 1°、Δρ ≤ 短辺の 1 %) | **91.7 %** | — |
| 三分割格子に対する **\|Δz\|** | **0.34** | **1.03** |

**一致率は高いが z は動く。**web-large が見つける直線はおおむね original でも見つかるが、
original では**さらに別の直線が出てくる**(5→15、3→7、11→20 の例がある)。
結果として z が最大 1.03 ずれる —— ④ の主張は群間の z の差で立てるので、
**解像度だけで 1 z 動くのは許容できない。**

**したがって解析は original を短辺 1024 へ縮小して行う。**web-large は使わない。

ICC は web-large / original とも**無い**(実測)。hanshoku-atlas が浮世絵で見た
「原寸の一部だけ sRGB.icc を持つ」構図とは異なる。構図解析は色に依存しないので、
本プロジェクトでは色管理は輝度変換の一点にのみ効く(§7 N-07)。

### 3.5 権利

Met Open Access は CC0(`isPublicDomain: true`)。NGA open data は CC0。
Wikimedia は作品ごとにライセンスを確認し、**PD / CC0 のみ**を採る。
**画像は再配布しない** —— URL を参照して閲覧者のブラウザが直接取得する(N-03)。
本プロジェクトが配るのは、そこから計算した数値だけである。

### 3.6 出典が違えば解析解像度が違う —— 群比較の交絡

§3.2 の実測で、Wikimedia 経由の既定は**短辺 1082–1490 px** に落ち着いた。
一方 Met の web-large は**短辺 419 px** である(§3.4)。**同じ検出器を当てても解像度が 3 倍違う。**

**Hough が拾う直線の本数は解像度の関数である見込みである。**そうであれば、出典の違う作品を
混ぜて ④ の群比較や ⑦ の作品くらべを行うと、**測っているのが構図でなく出典になる。**
[[hanshoku-atlas]] が §2.2 で館間の画像比較を捨てたのと同じ構造の交絡が、こちらでは
**解像度の側から来る。**

> **格下げして、実作品で戻した記録(2026-08-31 / HC-100)。**
> 初版はこれを断定で書いていた。L1 で測ると、同一の**合成**ステップエッジ画像を
> 短辺 419 / 1024 / 1490 で解析しても **検出直線数はいずれも 5 本で変わらなかった**(T-017)。
> 合成画像には筆触も細部も無いので、解像度を上げても新しいエッジが出てこない。
> そこで断定を見込みへ格下げし、規律(短辺の正規化)だけを残した。
>
> **L4 で実作品 12 件を測ると、断定の側が支持された**(§3.4)。
> 検出直線数は 5→15 / 3→7 / 11→20 と増え、三分割格子に対する z が最大 1.03 動く。
> **合成画像では見えず、実作品では見えた** —— 交絡は筆触・ひび割れ・細部から来ている。
> 根拠と規律を分けて扱ったから、規律を捨てずに済んだ(HC-100)。

**規律:** 解析はすべて**短辺を固定値へ正規化してから**行う。既定は短辺 1024 px。
正規化後の短辺が既定に届かない作品は、**群から外すか、届かなかったことを記録する**(F-07 / T-012c)。
この正規化を通っていない数を ④ / ⑦ に入れない。

## 4. 目玉 —— 分割比 t の走査曲線

「黄金比は名画に多いか」を、1/3 と 0.618 だけ測って答えてはならない。
**その二つを測ったという事実が、結論の形を先に決めてしまう**(循環)。

そこで測る対象を**比そのもの**にする。

> 縦線を x = t に置いたときの当てはまりスコア z を、**t ∈ [0.05, 0.95] を 0.005 刻み(181 点)で走査し、
> 曲線 z(t) を描く。**横線 y = t も同様。

三分割(t = 1/3)、黄金比(0.382 / 0.618)、√2 矩形(0.414)、√5 矩形(0.309)——
これらはすべてこの曲線上の**ただの一点**になる。判定は次のとおり。

- 曲線が**順列帰無の 95 % 帯**の中に全部収まる → **どの比も特別ではない**
- 帯の外に出る t がある → その位置を**事後に**読む。0.618 なら通説の裏付け、0.55 なら「黄金比ではない何か」

**どちらに転んでも出せる。**かつ、黄金比を優遇する経路が構造として存在しない。
③ の「調整すれば当たる」実演と ④ の統計が、この一本の曲線でつながる。

### 4.1 測った結果(L4b 実測 2026-08-31)

対象は Met European Paintings のパブリックドメイン絵画 **200 件**(層化抽出・規則は `data/frames/met-ep.json`)。
帰無は 1,000 枚・シード 20260831。

**曲線は中央に頂点を持つなだらかな単峰だった。**

| 向き | 頂点 | 頂点の z | t=1/3 | 0.382 | 0.5 | 0.618 | 2/3 |
|---|--:|--:|--:|--:|--:|--:|--:|
| 縦線 | **t = 0.465** | 0.227 | 0.012 | 0.155 | 0.200 | 0.140 | 0.035 |
| 横線 | **t = 0.515** | 0.202 | 0.044 | 0.132 | 0.161 | 0.163 | 0.091 |

**特別なのは中央だけである。**謳われた比のうち、三分割(1/3・2/3)は最も低い部類に入る。

この山は「画面の中央にエッジが集まる」という構造であって、**比の構造ではない**。
順列帰無の帯はこの山があるだけで大量の点を外すので(縦 121/181・横 124/181)、
それだけでは §4 の問いに答えられない。**なだらかな傾向(窓 0.3)を引いてから見る:**

| 向き | 三分割 1/3 | 三分割 2/3 | 黄金 0.382 | 黄金 0.618 | √2 | √5 | 中央 0.5 | 帯の外 |
|---|--:|--:|--:|--:|--:|--:|--:|--:|
| 縦線 | −0.023 | +0.009 | +0.071 | +0.063 | +0.019 | +0.053 | +0.044 | **10/181** |
| 横線 | +0.078 | −0.013 | +0.094 | +0.036 | +0.006 | +0.017 | +0.023 | **15/181** |

> **謳われた比は、ひとつも順列帰無の 95 % 帯を出なかった。**
> 曲線全体で帯を出たのは縦 10 点・横 15 点で、偶然に期待される 9 点とほぼ変わらない。

**これが ④ の答えである。**「差がほとんど無い」ことが成果だと §1 で書いたとおりになった。
三分割にだけ差が出て黄金にだけ出ない、という非対称も**無かった** —— どちらも帯の中である。

### 4.2 破壊しても下がらない(G-10 実測)

同じ絵を壊した版との**対応あり**の対比(三分割格子・200 対):

| 壊し方 | 平均差(A − C) | t | Cohen's d | Cliff's δ |
|---|--:|--:|--:|--:|
| トリム 20 % | **−0.302** | −5.38 | −0.390 | −0.211 |
| 回転 12° | **−0.303** | −6.04 | −0.382 | −0.229 |
| 鏡像 | −0.010 | −0.73 | −0.014 | −0.002 |

**壊した方がわずかに高い。**構図を壊せばスコアが下がる、という向きにはならなかった。

**鏡像がほぼ 0 なのは内部の妥当性検査になっている** —— 三分割格子は左右対称なので、
鏡像で関係が変わらないのが正しい。実際に変わらなかった。
**効くはずのないものが効かない**ことが確かめられたので、トリムと回転の差は器械の癖ではない。

### 4.3 自由度を上げると、対照群の方が大きく上がる(実測)

三分割格子の M5 − M0(調整を許したときの z の上昇):

| A(実作品) | C トリム | C 回転 | C 鏡像 | P(陽性対照) | D(無作為矩形) | E(1/f ノイズ) |
|--:|--:|--:|--:|--:|--:|--:|
| **1.89** | 2.24 | 2.55 | 1.88 | 0.00 | **4.29** | **2.96** |

**無作為矩形分割(4.29)と 1/f ノイズ(2.96)の方が、実作品(1.89)より大きく上がる。**
P 群が 0.00 なのは、最初から交点に載っていて調整の余地が無いからである。

これが §1 の批判の数値化である —— **「調整すれば当たる」は本当で、しかも
中身の無い画像ほどよく当たる。**ネット上の黄金螺旋図が M5 で当てながら M0 の顔をしているとき、
その図が示しているのは絵の性質ではない。

## 5. 当てはまりスコア

### 5.1 定義

特徴点集合 P(検出エッジ画素、または顕著性ピーク)と格子 G に対し

```
raw(P, G) = mean_{p∈P} exp( − d(p, G)² / 2σ² )      σ = 短辺の 1 %(既定・可変)
z(P, G)   = ( raw(P, G) − μ_rand ) / s_rand
```

μ_rand / s_rand は、**G と同じ本数・同じ角度分布のランダム配置格子 16,000 枚**(シード固定)の分布。

**枚数の出所(実測 2026-08-31)。**初版は 1,000 枚と書いていた。G-05 を測ると平均が −0.095 まで外れて落ちる。
原因は正規化ではなく **帰無を有限標本で推定する誤差**で、8 シードで測ると偏りは両側に散り
その平均は −0.0019(系統誤差ではない)。誤差は 1/√枚数 で縮み、1,000 枚では 0.032 ——
**閾値 0.05 に対して 1.6σ しかなく、通るかどうかがシード次第になる。**
16,000 枚まで上げると平均 −0.006 / −0.007 / +0.000、標準偏差 1.002 / 0.997 / 0.986(線数 4 / 8 / 20)。
閾値ではなく標本サイズを上げた。

**z には推定誤差が付く。**帰無を n 枚で推定した z の不確かさは概ね √(1/n + z²/2n) である。
画面はこの誤差を z と一緒に出す —— **帰無の枚数を減らせば z は速く出るが粗くなる**、という取引を隠さない(F-03)。

### 5.2 なぜ z 化が必須か

生スコアは**格子線が多いほど必ず高くなる**。Barnstone のアルマチュアは線が 20 本以上、
三分割は 4 本である。生スコアで両者を比べることは、線の本数を比べているに等しい。
z 化して初めて、三分割と黄金螺旋とアルマチュアが同じ尺度に乗る。

σ も結論を動かす。既定値は宣言値であり、**スライダーで動かせる**——
動かすと結論が変わることを見せるためである。④ の結論は σ の関数として一度描き、`/about` に載せる(F-16)。

### 5.3 自由度の会計

| モード | 許す変換 | DF |
|---|---|---|
| M0 | なし(固定格子) | 0 |
| M1 | t を最適化 | 1 |
| M3 | + 平行移動・拡大 | 3 |
| M5 | + 回転・鏡像 | 5 |

DF を上げればスコアは上がる。**その上昇量を対照群でも同じだけ測る。**
ネット上の黄金螺旋図は M5 で当てながら M0 の顔をしている、というのが §1 の批判の中身であり、
それを「M5 のとき 1/f ノイズも同じだけ上がる」という数として画面に出す。

## 6. 客観層のオラクル(循環の禁止)

客観層にも外部に正解は無い。**合成画像で作る**(hanshoku-atlas の F-02 と同じ構え)。

- **直線検出** — 既知の角度 θ・切片 ρ の **領域境界(ステップエッジ)** を描いた画像。
  Hough の (θ, ρ) が真値と一致するか(G-01)。
  **細線ではなくステップエッジである**理由(実測 2026-08-31 / HC-100):細線には両側にエッジが 2 本立つので、
  エッジ検出器は線の中心に線を置けない。しかも客観層が実際に測るのは §2 が挙げる
  「建築・地平線・卓の縁」= 領域境界であり、ステップエッジの方が対象に忠実である。
  ρ は θ 依存の支持関数 h(θ) = (W/2)|cosθ| + (H/2)|sinθ| に対する比で与える ——
  対角半径を一律の基準にすると、θ によっては線が画像の外へ出る
- **消失点** — **既知のカメラ行列で 3D 格子を投影した合成画像。**真の消失点が解析的に出る(G-07)
- **劣化耐性** — JPEG 品質・ノイズ・筆触テクスチャ・額縁の写り込みを段階的に加え、
  **どこで壊れるかを測って書く。閾値は置かない**(G-13)

**『最後の晩餐』の消失点がキリストの右目にある、は通説であって正解ではない。**
オラクルに使えば循環する。合成画像で較正した器械を名画に当て、出た数を出す。
通説との一致は**結果**であって前提ではない。

## 7. 画面

| | 画面 | 層 | ループ |
|---|---|---|---|
| ① | **線を見る** — Canny → Hough、角度別着色(水平/垂直/対角)、極座標ヒストグラム。**しきい値スライダーの隣に ③ のスコアを同時表示** | 客観 | L1 |
| ③ | **格子を重ねる** — 三分割/黄金分割/√2・√3・√5/対角線法/Hambidge/Barnstone/黄金螺旋、M0〜M5 | 規範 | L1 |
| ⑤ | **ノタン** — 2 値/3 値スライダー、明度重心マーカー、左右反転並置 | 客観 | L2 |
| ② | **消失点** — 直線群の交点クラスタリング。**破綻することが情報**(印象派・浮世絵・キュビスムで壊れる) | 客観 | L3 |
| ④ | **帰無仮説の実験室** — t 走査曲線 + 6 群の箱ひげ + DF 上昇量 | 両層の境界 | L4 |
| ⑦ | **作品くらべ** — 角度ヒストグラム・重心・対角優位度。**hanshoku-atlas と同一作品で接続** | 客観 | L5 |
| ⑥ | **視線の順路** | 保留(§9) | L5 |

**① と ③ を別画面にしないことが設計上の要点である。**しきい値を下げれば線は増え、上げれば減る。
「その絵の構図線」が分析者の選んだしきい値の関数であることを、スコアの動きとして同一視野に入れる。

## 8. 帰無仮説の実験室(④)の対照群

箱ひげ図の説得力は群の選び方で決まる。**6 群置く。**

| 群 | 中身 | 役割 |
|---|---|---|
| **P** | 主要素を厳密に 1/3 交点・0.618 交点へ置いた**合成画像** | **陽性対照。**ここが高く出なければスコアが機能していない → **④ ごと撤回**(G-08) |
| A | 名画(選定規則を明示、N = 200) | 実群 |
| B | 同館 PD 絵画の無作為抽出(層化・シード刻印、N = 200) | 「名画」と「絵画一般」の差 |
| **C** | **A を 20 % トリム / 回転 / 鏡像で壊したもの**(対応あり) | **最強の陰性対照。**画像統計を保ったまま構図意図だけが崩れる。ここで差が出なければ、スコアは構図を測っていない(G-10) |
| D | 無作為矩形分割(シード固定、N = 200) | |
| E | **1/f(ピンク)ノイズ**(N = 200) | ホワイトノイズは自然画像と統計が違いすぎて楽勝の対照になる。**1/f にする** |

### 8.1 検定力を先に宣言する

差の向きは予言できないが、**検出力は予言できる。**
「差が無かった」を「標本が小さかっただけ」と言われない構えを先に作る。

効果量 Cohen's d = 0.30 を α = 0.05(両側)・検出力 0.80 で検出するには **175 / 群**。
**N = 200 で確定する**(G-09)。C 群は対応ありなので同じ d でより少なくて済むが、A と対にするので 200。

### 8.2 「名画」の選定は循環しうる

選定規則(館・部門・PD・分類・抽出シード)を `data/frames/*.json` に刻む。
**枠を複数並走させ、枠を変えると結論が変わるかを見る。**変われば、それ自体が結果である。

## 9. ⑥ 視線の順路 —— 出さない可能性を先に持つ

spectral residual が測るのは「画像の統計的異常度」であって「人がどこを見るか」ではない。
視線計測データ無しに順路線を引けば、それは反証不能な図であり、**§1 で批判した当のものになる。**

**G-目玉2:** 公開視線データセットで AUC-Judd を測り、**≥ 0.75 なら出す。下回れば ⑥ ごと削る。**
較正できないものは画面に出さない。

## 10. 機能要求

| ID | 要求 | 優先度 | ループ |
|---|---|---|---|
| F-01 | **直線検出器。**Canny → Hough(投票・非極大抑制)。しきい値・σ・投票下限をすべて引数に取り、**既定値を定数として一箇所に持つ** | must | L1 |
| F-02 | **合成画像生成器。**既知の (θ, ρ) の線分・既知のカメラ行列の 3D 格子投影・既知の交点配置を生成し、劣化(JPEG 品質・ノイズ・筆触・額縁)を段階的に加えられる | must | L1 |
| F-03 | **当てはまりスコア器。**§5 の raw と z。ランダム格子 16,000 枚はシード固定。**z と一緒に推定誤差を返す** | must | L1 |
| F-04 | **格子生成器。**三分割/黄金分割/√2・√3・√5/対角線法/Hambidge/Barnstone/黄金螺旋。各格子は「線の集合 + 交点の集合 + 線数」を返す | must | L1 |
| F-05 | ① 線を見る。画像 URL 入力 → しきい値スライダー → 角度別着色 + 極座標ヒストグラム + **③ のスコア同時表示** | must | L1 |
| F-06 | ③ 格子重ね。M0〜M5 の切替。M5 は手動でも動かせる(「調整すれば当たる」を利用者に実演させる) | must | L1 |
| F-07 | 画像取得は `crossOrigin="anonymous"`。canvas を汚さずに `getImageData` する。Met / NGA / Wikimedia の 3 経路。Wikimedia は **§3.2 の白名簿 11 幅からのみ選び**、`要求幅 = 必要幅以上で最小の標準幅`、`要求幅 ≤ 原寸幅` を検算し、**400 と 404 を切り分ける** | must | L1 |
| F-08 | 全画面に**測っている対象の主語**を出す(§1 の一文を短くしない) | must | L1 |
| F-09 | ⑤ ノタン。2 値/3 値スライダー、明度重心マーカー、左右反転並置 | must | L2 |
| F-10 | **二実装照合。**Rust/WASM と TS で F-01 の投票配列・F-03 の z が一致する | must | L2 |
| F-11 | ② 消失点。直線群の交点クラスタリング。**推定の破綻を「破綻」として表示する**(平均で埋めない)。**位置の不確かさ(1σ)も返す** | must | L3 |
| F-12 | ④ **t 走査曲線。**181 点 × 順列帰無の 95 % 帯 | must | L4 |
| F-13 | ④ 対照群の箱ひげ図(6 群)+ 効果量(Cohen's d と Cliff's δ)+ DF 上昇量 | must | L4 |
| F-14 | 標本枠。**Met の objects エンドポイント**(2,644 件)の全メタデータを目録に残し、適格条件で絞って年代層別に抽出。**抽出規則と乱数シードをファイルに刻む** | must | L4 |
| F-15 | ⑦ 作品くらべ。角度ヒストグラム・重心・対角優位度。hanshoku-atlas と同一 objectID で接続 | should | L5 |
| F-16 | `/about` —— 二層の開示、σ を動かしたときの結論の変化、**落ちたゲートの記録**、Livio の批判の位置づけ | must | L6 |

## 11. 品質ゲート

**閾値は測る前に宣言する。落ちたら閾値を緩めず、主張の側を格下げする。**

| ID | 内容 | 閾値(宣言 2026-08-31) | 落ちたときの退避先 | ループ |
|---|---|---|---|---|
| G-01 | 無劣化の合成線分画像で Hough が真値を復元する | **Δθ ≤ 1.0° かつ Δρ ≤ 短辺の 0.5 %** | 検出器の欠陥として扱い、直すまで進まない | L1 |
| G-02 | **陰性対照。**投票を壊した版(累積を退化させる)で G-01 が落ちる | 落ちること | 検査が働いていないので、検査ごと捨てる | L1 |
| G-03 | **循環の禁止。**検出器・スコア器は真値を参照していない | 静的に確認 | 該当検査を無効と判定 | L1 |
| G-04 | **決定論。**同一入力・同一シードで検出結果と z がビット一致 | 100 回で不一致 0 件 | シード管理の欠陥として直す | L1 |
| G-05 | **z 正規化が線数の交絡を落とす。**線数 4 / 8 / 20 のランダム格子に対する z | **\|平均\| ≤ 0.05 かつ \|標準偏差 − 1\| ≤ 0.05** | 正規化を作り直す。**格子間の比較を撤回する** | L1 |
| G-06a | **二実装照合(整数と四則の区間)。**入力(エッジ座標・角度表)を固定し、**投票配列と極大**を比べる | **完全一致** + 経路をずらした陽性対照が落ちる | 一致させる規則を分離して書き直す | L2 |
| G-06b | **環境差の測定。**各実装が自前で角度表を計算したときの投票配列の差 | **閾値を置かない。ずれる件数と場所を測って書く** | —(測定であってゲートではない) | L2 |
| G-06c | **二実装照合(超越関数を通る区間)。**raw と z | **\|Δz\| ≤ 1e-9**(宣言 2026-08-31) | 一致しない原因を切り分け、許容の根拠を書き直す | L2 |
| ~~G-07a~~ | **撤回。**精度(画素)→ 精度(角度)→ 不確かさの較正、と三度立てて三度落ちた(§11.5) | — | — | L3 |
| G-07b | **検出率・被覆率・誤差の伸びの測定** | **閾値を置かない。数を記録する** | —(測定であってゲートではない) | L3 |
| G-07c | **破綻を破綻として返す。**平行投影で有限の消失点を返さない / 材料不足を埋めない | 有限を返したら落ちる | 推定器の欠陥として扱う | L3 |
| G-08 | **P 群(陽性対照)で M0 の z が高い** | **z ≥ 3.0** | **スコアが機能していない。④ ごと撤回する** | L4 |
| G-09 | **検定力。**d = 0.30 を α = .05 / 検出力 .80 で検出できる標本を確保している | **N ≥ 175 / 群** | 件数による主張を破棄し、**実例列挙**に格下げ | L4 |
| G-10 | **A 群と C 群(トリム版)の対比が検出可能。**対応ありの検定 | 検出力 0.80 を満たす対の数 | **「構図意図を測っている」の主張を撤回する** | L4 |
| G-11 | **層の混線禁止。**規範層の描画関数が実線を吐かない | 静的走査 + **陽性対照**(実線を吐く壊した版が落ちる) | 直す | 全 |
| G-12 | 字種検査。日本語本文に別字種・制御文字が混入しない | `harness/text_hygiene.py` が 0 件 | 直す | 全 |
| G-13 | **劣化耐性の測定。**JPEG 品質・ノイズ・筆触・額縁を段階的に加え、G-01 がどこで落ちるかを記録する | **閾値を置かない。壊れる境界を測って書く** | —(測定であってゲートではない) | L1 |
| **G-目玉1** | **t 走査曲線が順列帰無の 95 % 帯を出るか** | **閾値を置かない。出ても出なくても成果。**帯の外に出た t を**全部列挙する** | —(判定であって合否ではない) | L4 |
| **G-目玉2** | ⑥ の AUC-Judd | **≥ 0.75** | **⑥ を削除する** | L5 |

### 11.1 二実装照合で何に完全一致を要求できるか(実測 2026-08-31)

G-06 は初版で「投票配列が完全一致」とだけ書いていた。**これは測らずに宣言していた。**
Rust 1.71 と Node(V8)で、決定論標本 2,000 点の最終ビットを突き合わせた結果:

| 関数 | 不一致 | 理由 |
|---|--:|---|
| `sqrt` | **0 / 2000** | IEEE 754 が**正しい丸め**を義務づけている |
| `cos` | 44 / 2000 | 義務が無い。実装(fdlibm 系 / システム libm)ごとに最終 ULP が違う |
| `sin` | 50 / 2000 | 同上 |
| `exp` | **187 / 2000** | 同上。ずれる割合が最も大きい |

**帰結:二実装照合で完全一致を要求できるのは、整数・四則・`sqrt` だけで書かれた区間である。**
超越関数を通った時点で、言語間の一致は「要求するもの」ではなく **「測るもの」** になる。
角度表が 1 ULP ずれれば `round(ρ × rhoScale)` はビン境界で反転しうるので、
**入力を固定した比較(G-06a)と、各自が計算した場合の差の測定(G-06b)を分ける。**
`exp` を通る raw / z は完全一致を原理的に達成できないので、許容を宣言して比べる(G-06c)。

これは閾値の緩和ではない。**何に完全一致を要求できるかを、実測に合わせて切り直した**ものである。

## 11.2 測定値(L1 実測 2026-08-31)

閾値を持つゲートは通った値を、持たないゲートは測った境界を残す。

| ゲート | 測定 |
|---|---|
| G-01 | 900 ケース掃引(θ 0–179° × ρ 5 段階)で **最悪 Δθ = 0.091°(θ=135°)/ 最悪 Δρ = 0.663 px**(許容 1.0° / 1.92 px)。§11.4 の座標規約を直した後の値 |
| G-02 | 投票を θ ビン 1 本に潰した対照が落ちることを確認。対の正常系は同じ入力で復元 |
| G-03 | hough / canny / score / grids のいずれも synth・tests を参照しない(静的走査 0 件)。参照する版が検査に捕まることも確認 |
| G-04 | 検出 100 回・z 20 回でいずれも不一致 0 件 |
| G-05 | 線数 4 / 8 / 20 で **平均 −0.0056 / −0.0067 / +0.0004**、**標準偏差 1.0023 / 0.9971 / 0.9861**(許容 ±0.05)。線数を無視した帰無で測る陽性対照は \|z\| > 3 で落ちる |
| G-11 | SVG の線を描くファイルは `LineStage.tsx` の 1 件で、layerStyle を経由している |
| G-12 | 走査 30 ファイル / 違反 0 件 |
| G-13 | **閾値なしの測定。**JPEG 相当の量子化は品質 1 まで壊れず / ガウスノイズは **σ = 96 で壊れる** / 筆触は強度 12 まで壊れず / 額縁は **幅 25 % で壊れる**(いずれも 4 本のステップエッジを短辺 384 px で) |

### 11.3 測定値(L2 実測 2026-08-31)

| ゲート | 測定 |
|---|---|
| G-06a | 固定フィクスチャ(97×73・エッジ 248 点・角度表 180 本)に対し、**投票 22,140 ビンと極大 3 本が f64 のビットまで完全一致**。ρ ビンをずらした版・丸めの流儀を変えた版はいずれも落ちる |
| G-06b | **閾値なしの測定。**両実装が自前で角度表を計算すると **360 個中 8 個が最終 ULP で違う**。それでも **投票ビンの差は 0/22,140(最大差 0 票)** —— この解像度では ULP 差がビン境界を一度も跨がなかった。**「一致した」ではなく「この入力では跨がなかった」と読む** |
| G-06c | **Δraw = 0 / Δz = 0**(許容 1e-9)。`exp` は 187/2000 で食い違うが、この標本ではたまたま一致した。**許容を外す根拠にはしない** |
| Rust 側 | `cargo fmt --check` / `clippy -D warnings` / `cargo test` 5 件 / `wasm32-unknown-unknown` リリースビルド **23,878 B** |

**G-06b・G-06c の読み方。**どちらも「差が 0 だった」だけであって「差は原理的に出ない」ではない。
§11.1 の実測が示すとおり `cos` / `sin` / `exp` は言語間で最終 ULP が食い違う。
**この入力・この解像度で跨がなかった、が測ったことのすべてである。**
解像度を上げれば(rhoScale を上げれば)ビン境界は密になり、跨ぐ確率は上がる。

### 11.5 消失点は「画素で何 px 合っているか」では測れない(実測 2026-08-31)

G-07 の初版は **画素誤差 ≤ 短辺の 1 %** と書いていた。**消失点までの距離を縛らずに絶対画素で
精度を要求していた。**yaw を 45° から 5° まで振って測ると:

| 消失点までの距離 ÷ 画面対角 | 線束の広がり | 画素誤差 | 3D 方向の角度誤差 |
|--:|--:|--:|--:|
| 0.6 | 42.2° | 2.90 px | 0.179° |
| 1.0 | 30.0° | 5.10 px | 0.220° |
| 1.4 | 29.1° | 14.36 px | 0.465° |
| 2.2 | 13.0° | 56.01 px | 0.923° |
| 3.5 | 8.7° | 27.24 px | 0.217° |
| 7.0 | 5.1° | 190.42 px | 0.382° |
| 14.2 | 2.1° | 325.91 px | 0.165° |

**画素誤差は消失点が遠ざかると発散し、角度誤差は 1° 未満に収まる。**
遠い消失点はほぼ平行な線束の交点なので、位置の不確かさは 1/sin(線束の広がり) で増える ——
**これは推定器の欠陥ではなく幾何の性質である。**線束の広がりも 42.2° から 2.1° へ縮んでいる。

したがって物差しを **不変量である角度** に切り直した(G-07a)。画素誤差の側は
閾値を持たない測定として残す(G-07b)。**閾値の緩和ではなく、測る量の取り替えである。**

#### 精度の主張を三度立てて三度捨てた(2026-08-31)

| 立てた主張 | 検証 | 結果 |
|---|---|---|
| 画素誤差 ≤ 短辺の 1 % | 合成透視図 | **落ちた。**消失点が遠いほど発散する(14.2 対角で 326 px)。距離を縛らない絶対画素は物差しにならない |
| 3D 角度誤差 ≤ 1.0° | 較正 18 点 → **ホールドアウト 4 台** | **落ちた**(1.511°)。実装の欠陥を 1 つ直して通したが、**別の機で 2.044°** |
| 誤差 ≤ 申告 3σ が 95 % | シード生成 40 台 | **落ちた**(90.0 %)。ただし指標が二つのことを混ぜていた(下記) |

二番目で見つかった実装の欠陥は本物だった —— **二つの線束が角度域で重なるとき**
(実測では 89–93° を共有)、貪欲な逐次割り当てが共有された直線を先着で奪い、
両方の推定が汚れていた。全直線を割り当て直して再当てはめる交互最適化に直した。

**しかしそこで止めた。ホールドアウトを二度見た時点で、それはもうホールドアウトではない**(HC-106)。
さらに直して通せば、汎化の証拠ではなく当てはめの結果になる。

#### 混ぜていた二つを分けたら、こうなった(実測 2026-08-31)

三番目の指標は **検出率**(そもそも線束を見つけられたか)と **較正**(見つけたものの申告が正しいか)を
混ぜていた。片方の線束しか見つからないと、残った真値に無関係な消失点が対応づけられ、
比が 34,815σ まで飛ぶ。これは較正の破綻ではなく検出の失敗である。分けて測ると:

| シード | 検出率 | 対応づいた分の被覆率(3σ) | 最悪 |
|---|--:|--:|--:|
| 20260831(40 台) | **81.3 %**(65/80) | **96.9 %**(63/65) | 6.1σ |
| 20260901(40 台・別シード) | **82.5 %**(66/80) | **92.4 %**(61/66) | 6.0σ |

**推定器は、見つけたものについては自分の不確かさをおおむね正しく申告する。
そして 2 割弱を見つけ損なう。**別シードで再現するので、シードの産物ではない。

#### したがって ② が主張することは一つに絞られた

「消失点を精度よく求められる」ではなく、

> **平行投影のように消失点が定義できない絵では、有限の点を返さない。**
> 求まった場合も、どれだけ当てにならないかを数で添える。

これだけが L3 で立った主張である(G-07c)。**合成画像に対してすら精度を保証できなかった**という
事実は、実作品に対する消失点の主張がどれだけ弱いかを示す ——
この結果自体が §1 の方針(構図線がいかに容易に、そして雑に見つかるか)の一部である。

**あわせて推定器は位置の不確かさを返す**(F-11)。線束が細いと正規方程式が退化に近づき、
1σ の長軸が跳ね上がる —— 「消失点はここだ」と言えない状況を、言えないまま数で出す。

### 11.4 座標規約(L2 で直した)

画素 i が覆う区間は [i, i+1) なので中心は i + 0.5、画像 [0, size) の中心は size/2。
したがって中心原点座標は **i + 0.5 − size/2** である。

L1 の実装は `i − size/2` と書いており、**画素群の重心が原点から 0.5 px ずれていた**。
生成器と検出器が同じ原点を使っていたので G-01 は通っており、
**明度重心(F-09)の対称性の検査で初めて露見した。**
`src/core/image.ts` の `toCenter` に集約し、synth / hough / notan と Rust 側を揃えてある。
直した後の G-01 は最悪 Δθ = 0.091° / Δρ = 0.663 px(直前は 0.137° / 0.537 px)。

**G-13 の読み方。**ステップエッジは強い勾配を作るので、無地の合成画像に対する検出器は
きわめて頑健である。**これは実作品に対する頑健さを意味しない** —— 実作品には筆触・ひび割れ・
額縁・展示照明の写り込みがあり、それらは「壊す」のではなく **偽の線を足す**。
L3 で実作品を入れたときに、同じ軸を測り直す。

### 11.6 ④ の事前計算(L4b 実測 2026-08-31)

| 段 | 何をするか | 道具 |
|---|---|---|
| 1 | 実作品 200 点を Met から取得(original)し、**短辺 1024 へ縮小して復号** | Python(Pillow) |
| 2 | Canny → エッジ点を 2,000 点へ決定論的に間引く。**C 群(破壊版)もここで作る** | TS(`src/core`) |
| 3 | **格子を書き出す。**比の定数は規範層にだけ置くので、Rust には線の集合として渡す | TS(`grids.ts`) |
| 4 | 格子 9 種の z・走査曲線 181 点 × 2 向き・自由度の会計を全 1,400 件について計算 | Rust |
| 5 | 走査曲線を A 群と E 群だけに削り、3 桁に丸めて `src/data/scores.json` へ | Node |

**帰無の枚数は 1,000。**G-05 の宣言値 16,000 より少ないが、
z の推定誤差 0.032 は **群の平均(200 件)を取る段で √200 倍に薄まり 0.002 になる**。
群間の比較にはこれで足りる。ゲートを測るときは 16,000 のままである。

**取りこぼしは黙って落とさない。**取得できなかった作品は `data/points/manifest.json` の
`dropped` 欄に理由を残す(実測: 200 件すべて取得できた)。

> **一度取りこぼした記録。**格子を manifest の寸法欄から書き出したところ、
> **C 群のトリム(寸法が 0.8 倍に変わる)が丸ごと 200 件、採点から落ちた**。
> 既に手元にあった 3 件も、manifest に `cached` としか書かれず寸法を持たないので落ちた。
> **点集合のファイル自体が寸法を先頭 8 バイトに持っている** —— 派生した記録ではなく実物を見る。
> 気づけたのは「A 群と破壊版 3 種の数が揃う」という不変量をテストに書いていたからである。

## 12. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | **サーバ処理ゼロ。**Next.js 静的書き出し(`output: "export"`)のみ。API Route / Edge Function を持たない | `next build` の出力に関数が無いこと |
| N-02 | **課金経路ゼロ。**ビルド時に外部へ取りに行かない。実行時の外部取得は閲覧者のブラウザ ↔ 美術館のみ | ビルドをネットワーク無しで通す |
| N-03 | **画像を再配布しない。**URL 参照のみ。④ の対照群スコアだけを JSON で事前計算して配る | ビルド成果物の走査 |
| N-04 | ① の解析(短辺 1024 px 相当)が Web Worker で 1 秒以内 | 実測 |
| N-05 | **Rust/WASM は Hough 投票・極大抽出・z 正規化・FFT に限る。**Canny は合成オラクルで足りるので二重実装しない | 二実装照合の対象を F-10 に明記 |
| N-06 | モバイル対応。複数の画面幅で実際に見る(HC-078) | 実ブラウザ検品 |
| N-07 | 輝度変換の式を宣言し一箇所に持つ。ブラウザ差が出たら**画面に表示する。隠して平均しない** | Chrome / Firefox で同一画像の検出直線集合を比較 |

## 13. 公開の規律

**L3 完了時点では公開しない。**

L1〜L3 だけでも「よくできた構図分析ツール」にはなる。しかし ④ を欠いた状態のこのアプリは、
§1 で批判した対象そのものである。**④ が立ってから公開する。**

> **門を開けた(2026-08-31 / L4b)。**④ が立った ——
> 分割比の走査曲線(§4.1)、破壊版との対応あり対比(§4.2)、自由度の会計(§4.3)がそろい、
> **謳われた比はひとつも順列帰無の帯を出なかった**。
> 「差がほとんど無い」という結果そのものが §1 で宣言した成果である。
> このアプリはもう「黄金螺旋の量産機」と区別がつく —— 区別をつけているのは ④ の数である。

## 14. スコープ外

- **個別作品への「黄金比を使っている / いない」の判定。**§1 と正面から矛盾する
- 作者の意図の推定、美的評価、「良い構図」の判定
- 実際の視線計測(⑥ が落ちたら図ごと消す)
- **館間の画像比較。**撮影条件が交絡する(hanshoku-atlas §2.2 と同じ理由)
- 原寸画像の配布。短辺 1024 px で足りる(§3.4 で確かめる)
- 3 次元復元。② は消失点の推定までで、シーンの再構成には踏み込まない

## 15. ループ配分

| L | 射程 | ここで立つもの |
|---|---|---|
| L1 | F-01〜F-08 / G-01〜G-05・G-13 | 器械の較正が済む |
| L2 | F-09・F-10 / G-06a・G-06b・G-06c | 数が信用できるようになる |
| L3 | F-11 / G-07b・G-07c | **客観層の限界が分かる**(§11.5) |
| **L4** | **F-12〜F-14 / G-08・G-09・G-10・G-目玉1** | **アプリの主張がここで初めて立つ。公開はここから** |
| L5 | F-15 / G-目玉2(⑥ を出すか削るか) | hanshoku-atlas と接続 |
| L6 | F-16 | 開示 |

## 16. app-menu への掲載

- **カテゴリ:** cat15「🖼 美術・表象」を**新設**する。kozu-lab が第一作、hanshoku-atlas が第二作
- **技術タグ:** `画像処理`(**新設**)・`Rust/WASM`・`統計`
- **出典:** `美術館コレクション`(**新設**)・`Wikimedia`
- 掲載は **L4 完了後**(§13)
